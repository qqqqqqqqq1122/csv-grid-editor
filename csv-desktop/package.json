{
  "name": "csv-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "CSV Grid Editor Plus — Tauri 2.0 desktop shell (Rust + Node.js sidecar running csv-core)",
  "license": "MIT",
  "scripts": {
    "build:frontend": "node build-frontend.mjs",
    "build:sidecar": "node build-sidecar.mjs",
    "prepare-dev": "node build-frontend.mjs && node build-sidecar.mjs",
    "tauri": "tauri",
    "dev": "npm run prepare-dev && tauri dev",
    "build": "npm run prepare-dev && tauri build",
    "test": "node test/step010-frontend-assets.test.cjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.27.4"
  }
}
