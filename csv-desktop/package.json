{
  "name": "csv-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "CSV Grid Editor Plus — Tauri 2.0 desktop shell (Rust + Node.js sidecar running csv-core)",
  "license": "MIT",
  "scripts": {
    "build:frontend": "node build-frontend.mjs",
    "tauri": "tauri",
    "dev": "npm run build:frontend && tauri dev",
    "build": "npm run build:frontend && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "esbuild": "^0.27.4"
  }
}
